# ================================
# FILE: emotion_classifier.pkl
# ================================
# PURPOSE:
# Serialized trained model used to classify user emotions.
#
# TASKS FOR THIS FILE:
# 1. Store the final trained emotion model.
# 2. Be loaded during runtime for inference only.
#
# EXPECTED OUTPUT:
# - A trained ML model object
# - Compatible with vectorizer.pkl
#
# GENERATED BY:
# - src/emotion_detection/train_emotion_model.py
#
# USED BY:
# - src/emotion_detection/predict_emotion.py
# - src/app.py (indirectly)
#
# INTEGRATION NOTES:
# - This file should never be retrained during runtime.
# - If regenerated, integration must be informed.
#
# OWNER:
# ML Team (generation), Integration Team (usage)
#
# DO NOT:
# - Edit
# - Open manually
# - Commit multiple versions without cleanup
# ================================
